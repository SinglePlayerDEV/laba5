#include <iostream>
#include <cstdlib>
 

using namespace std;


int main()
{
    const int max{ 5 };
    int arr[max];
    int i{};
    cout << "Enter arr" << endl;
    while (i < max)
    {
        arr[i] = rand()%10;
       // cin >> arr[i];
        i++;
    }

    for (i = 0; i < max; i++)
        cout << "\t" << arr[i] << endl;
    
    int arr_min = 0;
    for (int i = 1; i < max; i++) 
    if (arr[i] < arr[arr_min])  arr_min = i;
    
    int arr_max = 0;

    for (int i = 1; i < max; i++)   

        if (arr[i] > arr[arr_max])  arr_max = i;
    
    swap(arr[arr_min], arr[arr_max]);

    for (int i = 0; i < max; i++)  cout << arr[i] << " " << endl;

        return 0;
    
}
